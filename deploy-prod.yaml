apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: enrober
  labels:
    name: enrober
spec:
  replicas: 1
  selector:
    matchLabels:
      name: enrober
  template:
    metadata:
      labels:
        name: enrober
        routable: "true"
      annotations:
        projectcalico.org/policy: "allow tcp"
        publicHosts: "ingress.apigee.com"
        publicPaths: "9000:/beeswax/deploy"
    spec:
      containers:
      - name: enrober
        image: jbowen/enrober:v0.0.2
        env:
          - name: DEPLOY_STATE
            value: "PROD"
          - name: JWTTOKENIMPL
            value: "test"
        ports:
          - containerPort: 9000
        imagePullPolicy: Always
